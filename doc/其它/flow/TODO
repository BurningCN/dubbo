// ====================================================================
todo  其他filter
todo CSConnectAndDisconnect
todo wrapper
todo threadLocal
todo admin 覆盖provider的逻辑
todo RegistryProtocol # export的覆盖逻辑
todo spi getActivateExtension
todo spi threadPool
todo 心跳
// ====================================================================
sd-transfer - provider 测试程序

todo ConfigurableMetadataServiceExporter serviceConfig.export();
todo HeeloService 和 GrettingSerive用的不同的registry
todo 多注册中心 -MultiRegistry -- done

// ====================================================================

// ====================================================================

// ====================================================================

pr - todo 

todo 修复ReadyTest、InternalThreadLocalMap
todo 修复setRegistry method
todo CacheListenerTest 继续添加

// ====================================================================
// ====================================================================
针对 GenericInvoke-flow 
1 泛化async的调用
2 GenericImplFilter#invoke的if分支大逻辑 , 两个filter的onReponse逻辑
3 GenericServiceImpl实现类
4 GenericServiceTest 如下的 test
Invoker<DemoService> invoker2 = protocol.refer(DemoService.class, url);
        GenericService client2 = (GenericService) proxyFactory.getProxy(invoker2, true);
        Object result2 = client2.$invoke("sayHello", new String[]{"java.lang.String"}, new Object[]{"haha"});
        这里是DemoService，不是GenericService，看和GenericInvoke-flow 里的有何区别
5 testGenericComplexCompute4FullServiceMetadata 复杂对象程序